{
  "pluginAlias": "Homebridge Seasons",
  "pluginType": "platform",
  "headerDisplay": "Displays the current season of the year as HomeKit contact sensors",
  "singular": true,
  "schema": {
    "type": "object",
    "required": ["name", "hemisphere"],
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Homebridge Seasons"
      },
      "hemisphere": {
        "title": "Hemisphere",
        "type": "string",
        "default": "north",
        "oneOf": [
          { "title": "Northern", "enum": ["north"] },
          { "title": "Southern", "enum": ["south"] }
        ]
      },
      "useAstronomicCalendar": {
        "title": "Use astronomic calendar",
        "type": "boolean",
        "default": false,
        "description": "When turned on, the plugin uses the solstices and equinoxes for precise seasonal dates. When turned off, the plugin uses fixed months like Dec-Feb for winter and Mar-May for spring."
      }
    }
  }
}
